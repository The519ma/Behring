# Behring Desktop

Electron shell for the VPS-hosted Behring workbench.

The app bundles the browser workbench for a stable first screen, but all operational routes point to the VPS at `http://217.15.167.222`.

Update feed:

- `http://217.15.167.222/software/desktop-updates`

Release artifacts are built by `.github/workflows/desktop-release.yml` when a tag matching `desktop-v*` is pushed.
